import { TrustResourcePipe } from './trust-resource.pipe';

describe('TrustResourcePipe', () => {
  it('create an instance', () => {
    const pipe = new TrustResourcePipe();
    expect(pipe).toBeTruthy();
  });
});
